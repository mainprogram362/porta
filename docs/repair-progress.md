# 精査指摘の修正進捗

2026-09-08開始。ユーザーは精査報告の修正を最後まで進めることを依頼済み。
クレジット等で中断した場合はこのメモと `docs/porta-review-2026-09-08.md` を読む。
既存の大量の未コミット変更を戻さない。アプリ設定・実データを検証に使わない。

## 実装・自動検証完了

1. コピー競合、部分失敗表示、移動・リネームの非上書き保証。
   - `foundation/safe_transfer.py` 実装・既存コピー接続済み。出力先内の専用一時領域でコピーし、renameat2(NOREPLACE)で公開。未対応時は失敗する。FIFO等を拒否し、コピー中の元更新も検査する。
   - 公開済み出力は後続失敗時に削除せず、部分結果として残す方針。競合した他者の出力を削除しないため。
   - 通常移動・リネーム・親移動を非上書きrenameへ接続。ZIPも専用一時領域で検査して公開。
   - 関連27テスト成功。新規 `test_safe_transfer.py` は競合ファイル／ディレクトリ／リンク、取消、同じサイズの異なる内容を検査。
   - 通常確認画面は `gui/operation_worker.py` で非同期化済み。取消をコピーのチャンク／操作の項目境界で受付。部分失敗を未実行と表示しない。失敗後の同じ確認画面での再実行は禁止。
   - `gui/work_lifecycle.py` とメニューで所有するQt処理の実行中終了を抑止。作業の命名・個別破棄を追加。GUI応答・取消・部分失敗・実行中終了・作業保持・命名の回帰試験を追加済み。
   - Undoを50段階・概算16MiBへ制限。別領域への移動はSHA-256照合と削除前の再照合・元の識別を実装。
   - 親フォルダ移動も競合非上書き、部分結果保持、元と出力の識別情報照合、部分結果の画面反映に統一。Undoで置き換わった別項目を移動しない回帰試験を追加。

## その他の実装・検証

2. README更新済み。公開済みの結果保持、取消、内容照合の追加読取負荷、異常終了時の専用一時領域の残存を説明。
3. 既知13失敗は仕様を照合して解消。実在パスの貼付前後余白と正式な空白名を区別し、設定パスは既存のルート基準解決を維持。雛形追加、未定義変数、旧ショートカット期待値等を整理。
4. `requirements-runtime.lock` に確認環境のPython依存を固定。bootstrapが参照。`requirements-test.lock` と通常の開発requirementsも整合。pytestを.venvに導入済みで/tmpの仮設依存は不要。pip check成功。
5. FMはQSplitterで一覧・出力先・リネーム欄を調整、狭い幅では縦配置。操作・状態欄を一覧スクロール外に保持。800×640の画像確認と600/960/1440幅・9/13ptテスト成功。
6. PathListInputは一括更新時のシグナル・列幅更新をまとめ、行の一括追加と均一行高で改善。1千件1.32秒、1万件29秒の参考測定（同時に利用者の別処理が動作、厳密な比較環境ではない）。1万件はなお待ち時間あり。全パスは保持。
7. 動画に編集後先頭15秒の試し変換を追加。通常と別名、CPU/GPU識別名、実行詳細の時間・サイズ。自動画質判定ではなく利用者が同条件で比較する。関連29試験成功。
8. 全体試験でQtの異常終了を確認。テストがcloseで隠した画面を後続のフォント変更試験まで保持していたため、`tests/conftest.py` で各テストの新規画面をdeferred deleteして分離。異常終了箇所を通過、572成功／非同期待ち時間の失敗1件。待機をQtイベントループに変更後、最終全体試験を再実行中。

## 最終検証

最終全体試験: **577 passed / 0 failed**（70.80秒）。ログ: `/tmp/porta-repair-complete.log`。
その後、結果表示がパス中のHTML風文字を装飾として解釈しないようプレーンテキストへ統一。関連 **13 passed**（3.41秒）。ログ: `/tmp/porta-repair-final-related.log`。
`pip check` と `git diff --check` も成功。利用者の作業データや実行中プロセスを操作せず、既存の未コミット変更を維持。
実行コマンド:

```sh
QT_QPA_PLATFORM=offscreen PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q -p no:cacheprovider
```

Wayland/X11の実デスクトップ操作、GPU実機での動画画質比較は未実施。
大規模一覧のモデル化・画面クラス全体の分割は今後の段階的改善。今回、実行・進捗・終了判定・安全な出力公開を別モジュールへ分離した。

## 追補: 内容基準のレイアウト（2026-09-08）

- `gui/responsive_grid.py` を追加。ボタンの文言・フォント・スタイルが返す必要寸法と現在幅から、列数・行高を自動計算する。メインメニューとカテゴリ画面へ適用。
- メインメニューの説明文は各ボタンのツールチップに保ち、入口を一覧できる密度に整理。メニューの最小寸法は現在内容を配置した結果から計算し、9ptで576×350、13ptで720×500を確認。どちらも縦横スクロールなし。極端なフォントでは画面の90%を上限にし、到達性のためスクロールへ切り替える。
- 機能画面の明示的な固定最低サイズを撤去。共通の文字基準操作下限とツールバーの必要幅だけを本体下限にし、各ページの最低寸法はスクロール領域内に閉じ込める。
- `gui/layout_policy.py` を追加。複数行入力と表はピクセルではなく表示行数を宣言し、フォント・DPI・枠から実寸を計算する。
- FM、メディア情報、環境確認、プロセス一覧等の表列は、見出し・代表値・Qtの内容幅から初期幅を決定。リネーム規則と対応表の抽出規則は幅に応じて折り返す。
- パス一覧のチェック・実行対象・削除ボタンは、固定pxではなく実際のボタン`sizeHint`から正方形・列幅を決める。フルパスと利用者による列幅調整は維持。
- 自動試験追加: responsive gridの1～3列再配置、9/13ptのメニュー最小表示でスクロールが0であること。
- 追補後の最終全体試験: **580 passed / 0 failed**（22.73秒）。ログ: `/tmp/porta-adaptive-final2.log`。`compileall` と `git diff --check` も成功。

開始時点のテスト: 545 passed / 13 failed。最新検証結果は作業ごとに追記する。
